<h1> SHOW de "Situation Actuelle" </h1>

<b>Liste des serres disponibles</b><br/>
<% unless @greenhouses.blank? %>
<ol>
	<% @greenhouses.each do |y| %>
					<li><%= y.id %> --- <% if @selected_greenhouse.id == y.id %> Serre actuellement selectionnée <% else %> <%= link_to "Choisir cette serre", show_current_state_pages_path(id: params[:id], selected_greenhouse: y.id) %><% end %></li>
<% end %>
</ol>
<% else %>
	Aucune serre n'est associée à cette école <br/>
<% end %>

<br/>
<br/>
<b>Liste des dernières valeurs</b><br/>
<table>
		<% @value_types.each do |y| %>
				<tr>
						<td>
										<%= VALUE_TYPES[:"#{y.name}"][0] %> :
						</td>
						<td>
								<%= instance_variable_get("@#{y.name}_values") %>
						</td>
				</tr>
		<% end %>
</table>


<%= link_to "Ancienne page d'accueil connecté", root_path(ana_display: "true") %>
