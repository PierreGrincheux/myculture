
<b><%= t('pages.chooseserre') %></b><br/><br/>

<ul class="nav nav-tabs ">
	<% @greenhouses.each do |y| %>			
		<li class="nav-item <%= "active" if @selected_greenhouse.id == y.id %>">
		<%= link_to "#{y.serial_nbr}", params.merge(id: params[:id], selected_greenhouse: y.id) %>
		</li>
	<% end %>
</ul>

